// components/service-worker-registration.tsx
"use client"

import { useEffect } from 'react'

export function ServiceWorkerRegistration() {
  useEffect(() => {
    // Temporarily disabled for landing page functionality
    // TODO: Re-enable when full app is ready
    console.log('Service worker registration temporarily disabled')
  }, [])

  return null
}

export function PWAInstallPrompt() {
  // Temporarily disabled for landing page functionality
  // TODO: Re-enable when full app is ready
  return null
}